<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MermentV1"
        tools:targetApi="31">


        <activity
            android:name=".ui.user.AboutUsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.UserActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.ShoppingCartActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.AddActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.CameraRecorederActivity"
            android:exported="false" />

        <activity
            android:name=".SignupActivity"
            android:exported="false" />
        <activity
            android:name=".SigninActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MermentV1.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/FullScreen" />
        <activity
            android:name=".ui.card.DetailActivity"
            android:exported="true" />

    </application>

</manifest>
